import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { Button } from '../src/core/Button';
import { Padding } from '../src/core/Padding';
import Application from "./context";

<Meta title="MDX|Button" component={Button} />

# Examples

With `MDX` we can define a story for `Button` right in the middle of our
markdown documentation.

<Preview>
  <Story name="states">
    <div>
      <Application>
        <div>
          <Padding>
            <Button state="brand" size="md">brand</Button>
            <Button state="default" size="md">default</Button>
            <Button state="primary" size="md">primary</Button>
            <Button state="secondary" size="md">secondary</Button>
            <Button state="success" size="md">success</Button>
            <Button state="danger" size="md">danger</Button>
            <Button state="warning" size="md">warning</Button>
          </Padding>
        </div>
        <br />
        <div>
          <Padding>
            <Button state="info" size="md">info</Button>
            <Button state="light" size="md">light</Button>
            <Button state="dark" size="md">dark</Button>
            <Button state="muted" size="md">muted</Button>
          </Padding>
        </div>
      </Application>
    </div>
  </Story>
</Preview>

# Props

<Props of={Button} />